import React, { Fragment } from "react";
import LoginForm from "../components/login-form";

const LoginPage = () => {
  return (
    <Fragment>
      <LoginForm />
    </Fragment>
  );
};

export default LoginPage;
