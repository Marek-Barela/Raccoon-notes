import React, { Fragment } from "react";
import Navigation from "../navigation";

const Dashboard = () => {
  return (
    <Fragment>
      <Navigation />
    </Fragment>
  );
};

export default Dashboard;
